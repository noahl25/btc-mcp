[
  {
    "title": "Crypto Tracker Agent",
    "description": "Tracks cryptocurrency prices, market trends, and portfolio performance in real-time.",
    "tools": {
      "get_crypto_price": "def get_crypto_price(symbol: str) -> dict:\n    \"\"\"\n    Retrieves the current price, market cap, and volume for a specified cryptocurrency.\n\n    Args:\n        symbol (str): The ticker symbol of the cryptocurrency (e.g., 'BTC').\n\n    Returns:\n        dict: A dictionary containing price, market cap, and 24h volume.\n    \"\"\"",
      "analyze_market_trends": "def analyze_market_trends(symbol: str, days: int) -> dict:\n    \"\"\"\n    Analyzes historical data and trends to predict potential price movements.\n\n    Args:\n        symbol (str): The ticker symbol of the cryptocurrency.\n        days (int): Number of past days to consider for trend analysis.\n\n    Returns:\n        dict: A dictionary containing trend indicators and predicted price movement.\n    \"\"\"",
      "portfolio_summary": "def portfolio_summary(portfolio: dict) -> dict:\n    \"\"\"\n    Generates a summary of a user's crypto holdings.\n\n    Args:\n        portfolio (dict): A dictionary mapping cryptocurrency symbols to amounts held.\n\n    Returns:\n        dict: Summary including total value, gains/losses, and allocation percentages.\n    \"\"\""
    },
    "cost_per_token": 0.0025,
    "creator": "user_pubkey_21",
    "port": 1044,
    "id": "agent_021",
    "private": false,
    "date": "2026-02-02T15:30:00Z",
    "embedding": [
      0.12,
      0.23,
      0.34,
      0.45
    ]
  },
  {
    "title": "Book Recommendation Agent",
    "description": "Suggests books based on user preferences, genres, and reading history.",
    "tools": {
      "recommend_books": "def recommend_books(user_id: str, n: int) -> list:\n    \"\"\"\n    Provides book recommendations tailored to the user's favorite genres.\n\n    Args:\n        user_id (str): Unique identifier of the user.\n        n (int): Number of book recommendations to generate.\n\n    Returns:\n        list: A list of recommended book titles.\n    \"\"\"",
      "create_reading_list": "def create_reading_list(user_id: str, duration: int) -> list:\n    \"\"\"\n    Generates a personalized reading list for a specified duration.\n\n    Args:\n        user_id (str): Unique identifier of the user.\n        duration (int): Number of days to plan the reading list for.\n\n    Returns:\n        list: Ordered list of books to read.\n    \"\"\"",
      "book_summaries": "def book_summaries(book_ids: list) -> dict:\n    \"\"\"\n    Returns brief summaries of recommended books.\n\n    Args:\n        book_ids (list): List of book identifiers.\n\n    Returns:\n        dict: Mapping of book_id to a short summary string.\n    \"\"\""
    },
    "cost_per_token": 0.0018,
    "creator": "user_pubkey_22",
    "port": 1045,
    "id": "agent_022",
    "private": false,
    "date": "2026-02-02T15:31:00Z",
    "embedding": [
      0.21,
      0.32,
      0.43,
      0.54
    ]
  },
  {
    "title": "Fitness Challenge Agent",
    "description": "Creates personalized fitness challenges to motivate users and track their performance.",
    "tools": {
      "generate_daily_challenge": "def generate_daily_challenge(user_id: str) -> dict:\n    \"\"\"\n    Creates a new daily workout challenge for a user.\n\n    Args:\n        user_id (str): Unique identifier of the user.\n\n    Returns:\n        dict: Challenge details including exercises, reps, and duration.\n    \"\"\"",
      "track_challenge_progress": "def track_challenge_progress(user_id: str, date: str) -> dict:\n    \"\"\"\n    Logs completion of exercises and tracks performance metrics.\n\n    Args:\n        user_id (str): Unique identifier of the user.\n        date (str): The date of the challenge.\n\n    Returns:\n        dict: Progress report including completed exercises and stats.\n    \"\"\"",
      "suggest_alternative_exercises": "def suggest_alternative_exercises(exercise_id: str, injury_type: str) -> list:\n    \"\"\"\n    Recommends alternative exercises for users with injuries.\n\n    Args:\n        exercise_id (str): Identifier of the original exercise.\n        injury_type (str): Type of injury restricting exercise.\n\n    Returns:\n        list: List of safe alternative exercises.\n    \"\"\""
    },
    "cost_per_token": 0.0020,
    "creator": "user_pubkey_23",
    "port": 1046,
    "id": "agent_023",
    "private": false,
    "date": "2026-02-02T15:32:00Z",
    "embedding": [
      0.15,
      0.25,
      0.35,
      0.45
    ]
  },
  {
    "title": "Event Planner Agent",
    "description": "Assists users in organizing events including scheduling, guest management, and budgeting.",
    "tools": {
      "schedule_event": "def schedule_event(event_name: str, date: str) -> dict:\n    \"\"\"\n    Creates an event schedule.\n\n    Args:\n        event_name (str): Name of the event.\n        date (str): Date of the event in YYYY-MM-DD format.\n\n    Returns:\n        dict: Event details including tasks and timeline.\n    \"\"\"",
      "manage_guests": "def manage_guests(event_id: str, guest_list: list) -> dict:\n    \"\"\"\n    Adds or updates guests for an event.\n\n    Args:\n        event_id (str): Identifier of the event.\n        guest_list (list): List of guest names or emails.\n\n    Returns:\n        dict: Updated guest list and RSVP status.\n    \"\"\"",
      "budget_tracker": "def budget_tracker(event_id: str, expenses: dict) -> dict:\n    \"\"\"\n    Tracks event expenses and remaining budget.\n\n    Args:\n        event_id (str): Identifier of the event.\n        expenses (dict): Dictionary of expense categories and amounts.\n\n    Returns:\n        dict: Budget summary including total spent and remaining.\n    \"\"\""
    },
    "cost_per_token": 0.0016,
    "creator": "user_pubkey_24",
    "port": 1047,
    "id": "agent_024",
    "private": false,
    "date": "2026-02-02T15:33:00Z",
    "embedding": [
      0.13,
      0.26,
      0.39,
      0.52
    ]
  },
  {
    "title": "Recipe Suggestion Agent",
    "description": "Suggests recipes based on ingredients, dietary preferences, and cooking time.",
    "tools": {
      "find_recipes": "def find_recipes(ingredients: list, diet: str) -> list:\n    \"\"\"\n    Recommends recipes based on available ingredients and dietary preferences.\n\n    Args:\n        ingredients (list): List of ingredients available.\n        diet (str): Dietary preference, e.g., 'vegetarian', 'vegan', 'keto'.\n\n    Returns:\n        list: List of suitable recipe names.\n    \"\"\"",
      "generate_shopping_list": "def generate_shopping_list(recipes: list) -> dict:\n    \"\"\"\n    Creates a shopping list for selected recipes.\n\n    Args:\n        recipes (list): List of recipes to prepare.\n\n    Returns:\n        dict: Mapping of ingredients to required quantities.\n    \"\"\"",
      "estimate_cooking_time": "def estimate_cooking_time(recipe_id: str) -> int:\n    \"\"\"\n    Estimates the cooking time for a recipe.\n\n    Args:\n        recipe_id (str): Identifier of the recipe.\n\n    Returns:\n        int: Cooking time in minutes.\n    \"\"\""
    },
    "cost_per_token": 0.0019,
    "creator": "user_pubkey_25",
    "port": 1048,
    "id": "agent_025",
    "private": false,
    "date": "2026-02-02T15:34:00Z",
    "embedding": [
      0.11,
      0.22,
      0.33,
      0.44
    ]
  }
]